import { FollowedMovieBack } from './followed-movie-back';

describe('FollowedMovieBack', () => {
  it('should create an instance', () => {
    expect(new FollowedMovieBack()).toBeTruthy();
  });
});
