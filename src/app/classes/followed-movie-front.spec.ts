import { FollowedMovieFront } from './followed-movie-front';

describe('FollowedMovieFront', () => {
  it('should create an instance', () => {
    expect(new FollowedMovieFront()).toBeTruthy();
  });
});
